import theme, { useTheme, Theme, makeStyles } from './theme';

import { useLoadFont, FontDefault } from '~/src/theme/typography';

export { theme, useTheme, Theme, makeStyles, useLoadFont, FontDefault };
