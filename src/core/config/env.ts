// Centralise all environment-level constants here.
// Swap the values out for react-native-config or a similar solution
// if you need per-environment builds.
export const ENV = {
  API_BASE_URL: 'https://api.example.com',
  API_TIMEOUT_MS: 15_000,
} as const;
